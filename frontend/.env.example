# Dirección del backend NestJS. Sin prefijo NEXT_PUBLIC_ a propósito: el
# navegador nunca la ve, porque habla con las rutas del propio Next y son
# ellas las que llaman al API guardando la sesión en cookies httpOnly.
API_URL=http://localhost:4000/api
